## 0. 前置驗證

- [ ] 0.1 手動以 `yt-dlp` 對一支目標影片取得音訊，記錄可輸出的格式選項
- [ ] 0.2 確認 `yt-dlp` 可作為 Tauri sidecar 打包（取得三平台執行檔、確認授權允許隨應用散布）
- [ ] 0.3 以一段已知內容的音訊實測 `voxnote_asr`，確認實際回應的分段皆含 `end`（程式碼顯示必定輸出，此為實機驗證），據以決定任務 1.2 的欄位型別
- [ ] 0.4 **決定客戶端是否需要 ffmpeg**：將 0.1 取得的音訊直接上傳 `voxnote_asr`，確認服務端可正常解碼。若可，客戶端不引入 ffmpeg（僅需 `yt-dlp` 一個 sidecar）；若不可，才加入客戶端 ffmpeg sidecar。結論記錄於 design.md

## 1. 客戶端解析段落結束時間

> 服務端已於 `normalize_segment()`（`server/app.py:324-333`）輸出 `end`，本節不需修改服務端。

- [ ] 1.1 於 `src-tauri/src/asr/mod.rs` 的 `LocalServerSegment` 新增 `end` 欄位
- [ ] 1.2 依任務 0.3 的實測結果決定型別：若各回應皆穩定含 `end` 則用 `f64` + `#[serde(default)]`，若可能缺漏則用 `Option<f64>`
- [ ] 1.3 確認既有批次逐字稿與即時字幕路徑的行為完全未變（兩者皆不讀取 `end`）

## 2. 音訊擷取

- [ ] 2.1 新增 `tauri-plugin-shell` 相依並於 `tauri.conf.json` 的 `plugins` 註冊（目前無此 plugin，執行 sidecar 必需）
- [ ] 2.2 於 `tauri.conf.json` 的 `bundle.externalBin` 註冊 `yt-dlp` sidecar
- [ ] 2.3 於 `src-tauri/capabilities/` 授予執行該 sidecar 的權限；權限範圍 MUST 限於所註冊的 sidecar，不得開放任意命令執行
- [ ] 2.4 新增 `src-tauri/src/video_subtitle/mod.rs`，實作以連結取得音訊
- [ ] 2.5 依任務 0.4 的結論處理音訊格式（若服務端可直接解碼則不轉檔）
- [ ] 2.6 實作暫存檔管理：處理完成或失敗皆清除
- [ ] 2.7 實作直播偵測，於處理前拒絕並回報改用即時字幕
- [ ] 2.8 實作連結無效／需登入／地區限制的錯誤回報

## 3. 字幕時間軸組裝

- [ ] 3.1 由轉錄回應的分段 `start`／`end` 組裝字幕時間軸結構
- [ ] 3.2 `end` 缺漏時標示該段時間不完整，不以字數推算填補
- [ ] 3.3 撰寫單元測試涵蓋：正常分段、`end` 缺漏、空分段

## 4. 落盤

- [ ] 4.1 新增影片項目資料表（來源 URL、標題、逐字稿、字幕時間軸、建立時間），與 `meetings` 分離
- [ ] 4.2 實作新增／查詢／刪除影片項目的資料層函式
- [ ] 4.3 確認影片項目不出現於會議記錄清單的任何查詢
- [ ] 4.4 於 `src/types/index.ts` 新增對應型別

## 5. 命令層與進度回報

- [ ] 5.1 新增 `src-tauri/src/commands/video_subtitle_cmds.rs`，提供處理、查詢、刪除等 command
- [ ] 5.2 沿用既有 `asr_progress` 事件回報階段（取得音訊／轉錄中／完成）
- [ ] 5.3 轉錄沿用批次所用的端點設定（gateway 部署下為 `/batch` 前綴），不另設影片專屬端點——影片轉錄與會議逐字稿同屬長音訊批次情境，適用同一模型與同一實例
- [ ] 5.4 於 `commands/mod.rs` 註冊新 commands

## 6. 階段一：浮動字幕視窗

- [ ] 6.1 新增 `src/pages/videoSubtitle.ts`：連結輸入、處理進度、逐字稿檢視
- [ ] 6.2 重用即時字幕的 overlay 視窗呈現，字幕來源改為時間軸推進
- [ ] 6.3 實作對齊起點的操作入口
- [ ] 6.4 實作字幕推進的暫停／繼續
- [ ] 6.5 確認置頂、移動、調整大小、點擊穿透、字級選項的行為與即時字幕一致
- [ ] 6.6 實作與即時字幕的雙向互斥：影片字幕顯示中拒絕啟動即時字幕，即時字幕進行中拒絕啟動影片字幕顯示
- [ ] 6.7 確認影片的擷取與轉錄**不**受此互斥限制，可與即時字幕 session 並行

## 7. 摘要

- [ ] 7.1 決定摘要的資料歸屬方案（見 design.md「摘要的資料歸屬」；`summaries` 對 `meetings(id)` 有 FK，無法直接沿用）
- [ ] 7.2 依 7.1 的方案實作影片摘要的儲存與查詢
- [ ] 7.3 重用既有的摘要 LLM 呼叫邏輯，不另建一套 prompt 或呼叫路徑
- [ ] 7.4 確認摘要為使用者主動觸發，非自動執行
- [ ] 7.5 確認既有會議摘要的行為完全未變

## 8. 階段一實測

- [ ] 8.1 以一支中文影片實測完整流程，確認字幕內容與時間軸正確
- [ ] 8.2 以一支英文影片實測
- [ ] 8.3 實測長影片（30 分鐘以上）的處理時間與逾時行為
- [ ] 8.4 實測直播連結的拒絕行為
- [ ] 8.5 實測無效連結的錯誤訊息
- [ ] 8.6 實測重新開啟已處理影片不觸發重新轉錄
- [ ] 8.7 確認既有即時字幕、批次逐字稿、會議記錄流程皆無回歸

## 9. 非同步佇列整合

> 若依建議排序（本變更於 `add-async-transcription-queue` 之後），第 5 節的轉錄呼叫
> 應**直接**以任務佇列實作，本節即為確認而非改寫。若本變更先行，則本節為必要的改寫。

- [ ] 9.1 影片轉錄沿用該變更的任務佇列與輪詢流程
- [ ] 9.2 確認未另建第二套任務執行機制
  - 本變更不修改 `server/app.py`，與 `add-async-transcription-queue` 無檔案衝突。惟該變更將轉錄端點改為非同步（breaking），本流程的轉錄呼叫需一併改為建任務＋輪詢。

## 11. 文件

- [ ] 11.1 於 README 說明本功能、其與即時字幕的分工，以及不支援直播的限制
- [ ] 11.2 說明 `yt-dlp` sidecar 的存在與更新方式
