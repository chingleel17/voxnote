## 0. 前置驗證

- [ ] 0.1 以 `yt-dlp` 實測一般 YouTube 影片、播放清單中的單一影片、直播、私人影片與無效 URL，記錄可用的中繼資料及錯誤型態
- [ ] 0.2 確認 `yt-dlp` 可作為 Tauri sidecar 於目標平台打包與散布，並記錄更新方式
- [ ] 0.3 將取得的音訊直接送入 `voxnote_asr`，確認服務端可解碼且每段實際回應皆含 `start`／`end`
- [ ] 0.4 依 0.3 決定 `end` 欄位型別，並確認客戶端是否可免除 ffmpeg；將結論更新至 design.md
- [ ] 0.5 建立最小 YouTube IFrame Player API spike，於 Tauri 開發及正式打包環境確認可播放、可取得 `getCurrentTime()`、狀態及錯誤事件
- [ ] 0.6 驗證影片禁止嵌入、被移除與私人影片的實際播放器錯誤，確認介面可區分處理

## 1. 轉錄時間軸

- [ ] 1.1 於 `src-tauri/src/asr/mod.rs` 的 `LocalServerSegment` 新增 `end` 欄位
- [ ] 1.2 由後端分段 `start`／`end` 組裝字幕時間軸；缺漏時標示時間不完整，不以字數推算
- [ ] 1.3 撰寫單元測試涵蓋正常分段、缺漏或無效時間、空分段及重疊分段
- [ ] 1.4 確認既有批次逐字稿與即時字幕行為未變

## 2. YouTube 音訊擷取

- [ ] 2.1 新增 `tauri-plugin-shell` 並註冊 `yt-dlp` sidecar
- [ ] 2.2 於 `src-tauri/capabilities/` 僅授予執行該 sidecar 所需權限，不允許任意 shell command
- [ ] 2.3 新增 `src-tauri/src/video_subtitle/mod.rs`，解析並正規化單一 YouTube 影片 URL、影片 ID、標題與來源 URL
- [ ] 2.4 於下載前拒絕非 YouTube URL、直播及預定直播
- [ ] 2.5 實作音訊取得及依前置驗證結論決定格式處理；非必要不得加入 ffmpeg
- [ ] 2.6 處理無效 URL、需登入、私人影片、地區限制及擷取失敗，回傳可呈現的錯誤類型
- [ ] 2.7 確保處理成功、失敗或取消後均清除暫存音訊

## 3. 非同步處理與進度

- [ ] 3.1 沿用 `add-async-transcription-queue` 的批次任務建立、輪詢、取消及逾時機制，不另建影片專屬佇列
- [ ] 3.2 回報驗證來源、取得音訊、等待轉錄、組裝時間軸與完成等階段
- [ ] 3.3 重新開啟已完成項目時直接讀取落盤結果，不建立新轉錄任務

## 4. 落盤與摘要

- [ ] 4.1 新增影片項目資料表，保存 UUID、YouTube URL、影片 ID、標題、逐字稿、字幕時間軸及時間欄位
- [ ] 4.2 新增影片摘要表，FK 指向影片項目，並保存既有摘要所需中繼資料
- [ ] 4.3 實作影片項目與摘要的新增、查詢及刪除資料層函式
- [ ] 4.4 確認刪除影片項目會一併刪除其摘要，且影片項目不出現在會議查詢中
- [ ] 4.5 於 `src/types/index.ts` 新增對應型別
- [ ] 4.6 重用既有摘要 LLM 呼叫邏輯，僅於使用者主動要求時產生摘要

## 5. Commands 與影片管理介面

- [ ] 5.1 新增 `src-tauri/src/commands/video_subtitle_cmds.rs`，提供處理、進度／取消、查詢、刪除及摘要 commands
- [ ] 5.2 透過 `src/api/` 新增前端 invoke wrappers，不在頁面直接呼叫 `invoke()`
- [ ] 5.3 註冊 commands 與必要後端 state
- [ ] 5.4 新增 `src/pages/videoSubtitle.ts`，提供 YouTube URL 輸入、處理進度、項目清單、逐字稿及摘要操作
- [ ] 5.5 介面明確標示僅支援非直播 YouTube 影片

## 6. 獨立內嵌播放視窗

- [ ] 6.1 新增由本地打包頁面構成的獨立 Tauri 播放視窗，不直接將 YouTube 網站設為 WebView URL
- [ ] 6.2 為播放視窗設定獨立 label、生命週期及最小 capability，確認 YouTube iframe 無法使用 Tauri IPC 或檔案權限
- [ ] 6.3 以 YouTube IFrame Player API 載入已儲存影片，並處理 ready、state change、playback rate 與 error 事件
- [ ] 6.4 已有同一影片播放視窗時聚焦既有視窗，避免建立重複實體
- [ ] 6.5 實作關閉播放視窗後的事件與資源清理，不刪除影片項目

## 7. 字幕同步與退回操作

- [ ] 7.1 以播放器 `getCurrentTime()` 作為唯一同步時間，顯示符合 `start <= currentTime < end` 的字幕
- [ ] 7.2 確認播放、暫停、緩衝、跳轉及倍速播放時字幕皆依媒體時間正確同步
- [ ] 7.3 對時間不完整或當下無字幕的區間提供明確且穩定的顯示行為
- [ ] 7.4 處理禁止嵌入、影片不存在、私人影片及播放器載入失敗，呈現可理解的原因
- [ ] 7.5 提供以系統預設瀏覽器開啟原始 YouTube URL 的按鈕，並標示外部播放不提供同步字幕
- [ ] 7.6 不實作浮動字幕、手動對齊、瀏覽器選擇器或外部播放狀態整合

## 8. 驗證

- [ ] 8.1 為 URL 正規化、直播拒絕、時間軸組裝、資料 CRUD 與字幕時間查找撰寫單元測試
- [ ] 8.2 以中文與英文 YouTube 影片實測完整處理、重新開啟及字幕同步
- [ ] 8.3 實測暫停、緩衝、跳轉、0.5x／1x／2x 播放速度及影片結束
- [ ] 8.4 實測 30 分鐘以上影片的進度、取消、逾時與暫存檔清理
- [ ] 8.5 實測直播、非 YouTube URL、私人／地區限制、禁止嵌入及已移除影片的錯誤與退回操作
- [ ] 8.6 於各目標平台的正式打包版本確認播放器載入、媒體播放與 capability 邊界
- [ ] 8.7 確認會議記錄、批次轉錄、摘要與即時字幕流程無回歸

## 9. 文件

- [ ] 9.1 於 README 說明 YouTube 影片字幕流程、不支援直播／其他平台及禁止嵌入時的限制
- [ ] 9.2 說明 `yt-dlp` sidecar 的存在、更新方式與授權注意事項
