## 1. 批次匯入後端

- [ ] 1.1 在 Rust 與 TypeScript 定義批次匯入 item、逐檔成功／失敗結果及整批結果型別，欄位命名與 serde camelCase 對應一致
- [ ] 1.2 從 `import_recording_file` 抽出可重用的單檔匯入 helper：驗證來源存在且為一般檔案、建立錄音目錄、產生保留副檔名的唯一目的檔名並複製檔案
- [ ] 1.3 讓單檔 helper 在資料庫建立錄音失敗時嘗試清除剛複製的目的檔，並以可理解的繁體中文錯誤回報失敗原因
- [ ] 1.4 在 `recording_cmds.rs` 實作 `import_recording_files`：一次取得 `DataOperationLock`，依輸入順序逐項匯入，單項失敗時記錄結果並繼續處理後續項目
- [ ] 1.5 確認批次匯入透過 `recording::create_recording()` 依序接續既有 `sort_order`，且不修改既有錄音段落
- [ ] 1.6 在 `src-tauri/src/lib.rs` 註冊 `import_recording_files`，並保留既有 `import_recording_file` 相容錄音頁單檔流程
- [ ] 1.7 為目的檔名與逐項處理 helper 新增 Rust 單元測試，涵蓋多檔唯一名稱、大小寫副檔名、無副檔名、來源不存在及單項失敗後繼續處理

## 2. 前端 API 與待上傳狀態

- [ ] 2.1 在 `src/api/recordings.ts` 新增 `importRecordingFiles(meetingId, items)` wrapper，所有頁面仍只透過 API 層呼叫 Tauri command
- [ ] 2.2 在 `renderMeetingPage` 建立可跨錄音區塊重建保留的待上傳狀態，項目包含來源路徑、原始檔名與可選錯誤訊息
- [ ] 2.3 實作待上傳項目的追加與同批次路徑去重；重新開啟檔案選擇器時追加新項目，取消選檔時維持原清單
- [ ] 2.4 實作批次結果套用：成功項目從待上傳狀態移除、失敗項目保留錯誤，重試時只送出仍在清單內的項目

## 3. 會議頁行內上傳介面

- [ ] 3.1 調整 `buildRecordingSection()` 的空狀態 early return，使無錄音與已有錄音兩種狀態的 header 都提供「上傳音訊」入口
- [ ] 3.2 使用 Tauri dialog `multiple: true` 與既有音訊副檔名 filter，一次加入多個檔案，且不得設定 `window.location.hash` 或導向錄音頁
- [ ] 3.3 建立行內待上傳面板，逐項顯示原始檔名與錯誤訊息，並提供「加入更多檔案」、逐檔「移除」、「取消全部」及「儲存錄音」操作
- [ ] 3.4 實作逐檔移除與取消全部：只改變待上傳狀態，不呼叫後端、不刪除來源檔或既有錄音；清單為空時停用儲存
- [ ] 3.5 儲存期間停用選檔、移除、取消與重複送出操作並顯示處理中狀態，完成或失敗後恢復可用狀態
- [ ] 3.6 批次至少一項成功後重新取得該會議錄音，留在原會議頁重建錄音與逐字稿相關區塊；若有失敗項目則保留並重新顯示待上傳面板
- [ ] 3.7 以 toast 回報全數成功、部分成功與全數失敗的數量摘要，並確保項目級錯誤可供使用者判斷重試或移除
- [ ] 3.8 在 `src/styles.css` 新增符合既有 detail section 視覺語言的待上傳清單、錯誤狀態與行動列樣式，確認窄視窗下可換行且操作按鈕可點選

## 4. 驗證

- [ ] 4.1 執行 Rust 單元測試及 `cargo check`，確認批次命令、檔名產生與錯誤清理流程通過且未新增 `unwrap()`
- [ ] 4.2 執行 `npm run build`，確認 TypeScript 型別檢查與 Vite build 通過
- [ ] 4.3 實測無錄音會議：在會議頁一次選取多檔、移除其中一檔、儲存其餘檔案，確認未跳頁且段落順序符合待上傳清單
- [ ] 4.4 實測已有錄音會議：新增多檔後確認新段落接續既有段落，既有順序、逐字稿與錄音內容不變
- [ ] 4.5 實測追加選檔、重複路徑去重、取消系統選檔、移除最後一項及取消全部，確認儲存前未建立 DB 記錄或複製檔案
- [ ] 4.6 實測部分失敗與全數失敗：確認成功段落不回滾、不重複匯入，失敗項目與錯誤保留且可重試或移除
- [ ] 4.7 實測大量／大型音訊上傳時 webview 不讀取音訊位元組，儲存期間不能修改清單或重複送出，完成後 UI 可繼續操作
- [ ] 4.8 回歸驗證錄音頁的桌面錄製、單檔上傳與儲存後導回會議頁流程仍正常
