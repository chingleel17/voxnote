# Tasks

## 1. 服務端回傳對齊狀態

- [x] 1.1 `WhisperXTranscriber.transcribe()` 回傳詞級對齊是否完成（現有 `aligned` 區域變數，`app.py:358-377`），與分段結果一併向上傳遞
- [x] 1.2 `build_transcription_response()` 於回應加入降級標示欄位，與既有 `diarization_enabled` 並列
- [x] 1.3 非同步任務完成結果納入同一欄位，確認與同步模式輸出格式一致
- [x] 1.4 未啟用語者分離時該欄位不影響既有輸出

## 2. 服務端測試

- [x] 2.1 `server/test_app.py` 新增測試：對齊成功時回應標示未降級
- [x] 2.2 新增測試：對齊拋出例外時仍完成轉錄，且回應標示已降級
- [x] 2.3 新增測試：同步與非同步兩種模式的降級標示一致

## 3. Rust 客戶端解析與保存

- [x] 3.1 `LocalServerTranscription` 新增選填欄位，沿用 `#[serde(default)]` 慣例（`asr/mod.rs:20-25`）
- [x] 3.2 `recordings` 表以 `ALTER TABLE` 新增欄位，加入 `db/mod.rs` 既有的向下相容 migration 區塊（第 194 行起）
- [x] 3.3 `Recording` struct 新增對應欄位；轉錄完成寫入錄音段落時一併保存
- [x] 3.4 確認欄位納入 `backup.rs` 的備份還原範圍

## 4. 前端型別與呈現

- [x] 4.1 `src/types/index.ts` 的 `Recording` interface 同步新增欄位
- [x] 4.2 會議頁錄音段落顯示可信度提示，措辭表達為品質標示而非錯誤
- [x] 4.3 講者對應區顯示對應提示，指引優先確認該段落講者標籤
- [x] 4.4 未啟用語者分離或未降級時不顯示提示
- [x] 4.5 `styles.css` 補上提示樣式，與既有提示元件視覺一致

## 5. 相容性驗證

- [x] 5.1 舊版服務端回應（無新欄位）搭配新版 app：視為未降級，不顯示提示
- [x] 5.2 舊資料庫升級後既有錄音段落取得預設值，不影響既有會議顯示
- [x] 5.3 AssemblyAI 供應商路徑不受影響，不顯示相關提示

## 6. 端到端驗證

- [x] 6.1 以正常音檔轉錄，確認不顯示提示
- [x] 6.2 以觸發對齊失敗的條件轉錄，確認提示出現於錄音段落與講者對應區
- [x] 6.3 重新開啟會議確認提示持久化（非僅轉錄當下顯示）
