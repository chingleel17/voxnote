## 0. 前置條件

- [ ] 0.1 確認 `add-live-caption-overlay` 任務 10 的轉錄正確性問題已解決且字幕輸出穩定，再開始本變更（見 proposal 的排程建議）

## 1. 擷取層重構（不改變既有行為）

- [ ] 1.1 將 `start_windows_loopback_capture` 中取得並初始化 `AudioClient` 之後的讀取迴圈抽為共用函式，參數含 client、目標取樣率、實際取樣率、佇列、錯誤狀態、running 旗標與音量值
- [ ] 1.2 既有裝置 loopback 改為呼叫該共用函式，確認迴圈內邏輯逐行未變（特別是 `:721-726` 跨批餘數保留的重採樣處理）
- [ ] 1.3 手動實測既有桌面錄音三種模式（僅麥克風／僅電腦音訊／混音）確認無回歸
- [ ] 1.4 手動實測即時字幕的「電腦音訊（全系統）」來源確認無回歸

## 2. process loopback 擷取路徑

- [ ] 2.1 新增以行程 ID 為目標的擷取函式，以 `AudioClient::new_application_loopback_client(process_id, true)` 建立 client
- [ ] 2.2 以 `desired_format`（16 kHz、雙聲道、f32）與 `autoconvert: true` 初始化，`buffer_duration_hns` 傳入自訂值（不呼叫 `get_device_period()`，該方法於此模式不可用）
- [ ] 2.3 不呼叫 `get_mixformat()`，取樣率以 `desired_format` 為準（初始化成功即代表格式生效）
- [ ] 2.4 匯流至任務 1.1 的共用讀取迴圈
- [ ] 2.5 實作 Windows 版本檢查，低於 10 版本 2004（build 19041）時回報該來源不可用
- [ ] 2.6 實作目標行程結束的偵測，寫入既有 `SharedError` 由字幕迴圈結束 session 並回報

## 3. 可選應用程式列舉

- [ ] 3.1 實作列舉目前有音訊工作階段的應用程式，取得行程 ID 與可辨識名稱
- [ ] 3.2 過濾掉本應用程式自身與無名稱的項目
- [ ] 3.3 新增列舉可選應用程式的 Tauri command
- [ ] 3.4 清單為空時回傳可供前端呈現說明的結果，而非空陣列無區別

## 4. 設定與型別

- [ ] 4.1 於 `AppConfig` 新增目標應用程式的設定欄位（行程 ID 與顯示名稱）
- [ ] 4.2 於 `src/types/index.ts` 同步新增對應型別欄位
- [ ] 4.3 於 `live_caption/mod.rs` 的 `validate_audio_source` 新增此來源，並於未選定目標時拒絕啟動
- [ ] 4.4 於 session 啟動分支新增此來源的擷取路徑

## 5. 前端介面

- [ ] 5.1 於 `src/components/liveCaptionSettings.ts` 的音訊來源選單新增「指定應用程式」
- [ ] 5.2 實作應用程式挑選介面（以名稱呈現，含重新整理）
- [ ] 5.3 加入提示文案：擷取範圍為整個應用程式而非單一分頁
- [ ] 5.4 加入提示文案：需先讓目標應用程式開始播放聲音才會出現在清單中
- [ ] 5.5 清單為空時顯示說明而非空白

## 6. 實測

- [ ] 6.1 以瀏覽器播放影片為目標實測，確認可擷取到聲音（驗證 `include_tree = true` 對多行程瀏覽器有效）
- [ ] 6.2 實測目標應用程式播放時，其他應用程式同時發聲不影響字幕內容
- [ ] 6.3 實測未選定目標即啟動時的拒絕行為與錯誤訊息
- [ ] 6.4 實測 session 進行中關閉目標應用程式的處理
- [ ] 6.5 實測切換回「電腦音訊（全系統）」與「麥克風」來源皆正常

## 7. 文件

- [ ] 7.1 於 README 的即時字幕說明加入此音訊來源，並標註 Windows 版本需求與「應用程式層級、非分頁層級」的能力邊界
