## 0. 前置條件

- [x] 0.1 確認 `add-live-caption-overlay` 任務 10 的轉錄正確性問題已解決且字幕輸出穩定，再開始本變更（見 proposal 的排程建議）
  - 已解除：取樣率錯配（loopback 音訊被重複降取樣）與幻覺過濾已於 commit `06233f6` 修正，該變更已歸檔。

## 1. 擷取層重構（不改變既有行為）

- [ ] 1.1 將 `start_windows_loopback_capture` 中取得並初始化 `AudioClient` 之後的讀取迴圈抽為共用函式，參數含 client、目標取樣率、佇列、錯誤狀態、running 旗標與音量值
- [ ] 1.2 既有裝置 loopback 改為呼叫該共用函式，確認 f32 雙聲道讀取、單聲道混合、約 100 毫秒批次推送與音量更新不變，且不新增第二次重採樣或第二個實際取樣率來源
- [ ] 1.3 手動實測既有桌面錄音三種模式（僅麥克風／僅電腦音訊／混音）確認無回歸
- [ ] 1.4 手動實測即時字幕的「電腦音訊（全系統）」來源確認無回歸

## 2. process loopback 擷取路徑

- [ ] 2.1 新增以行程 ID 為目標的擷取函式，以 `AudioClient::new_application_loopback_client(process_id, true)` 建立 client
- [ ] 2.2 以 `desired_format`（16 kHz、雙聲道、f32）與 `autoconvert: true` 初始化，`buffer_duration_hns` 傳入自訂值（不呼叫 `get_device_period()`，該方法於此模式不可用）
- [ ] 2.3 不呼叫 `get_mixformat()`，取樣率以 `desired_format` 為準（初始化成功即代表格式生效）
- [ ] 2.4 匯流至任務 1.1 的共用讀取迴圈
- [ ] 2.5 實作 Windows 版本檢查，低於 build 20348 時回報該來源不可用
- [ ] 2.6 建立 capture client 前複核目標 PID 仍存在且應用程式識別資訊未變，避免 PID 重用造成誤擷取
- [ ] 2.7 實作目標根行程結束的偵測，寫入既有 `SharedError` 由字幕迴圈結束 session 並回報；應用程式仍存在但暫時沒有音訊時視為靜音，不得結束 session

## 3. 可選應用程式列舉

- [ ] 3.1 實作跨作用中輸出端點列舉目前正在輸出音訊的 session，取得音訊行程 ID
- [ ] 3.2 將音訊行程解析為可涵蓋應用程式行程樹的根目標，以穩定應用程式識別資訊去重，並取得可辨識名稱
- [ ] 3.3 過濾本應用程式自身、無名稱及無法安全解析為應用程式目標的項目
- [ ] 3.4 新增列舉可選應用程式的 Tauri command，回傳根 PID、顯示名稱及供啟動前複核的非機密識別資訊
- [ ] 3.5 清單為空時回傳可供前端呈現說明的結果，而非空陣列無區別

## 4. 設定與型別

- [ ] 4.1 將 `AppConfig.live_caption_audio_source` 與前端對應型別擴充為支援 `application`；不得新增持久化 PID 或應用程式名稱欄位
- [ ] 4.2 於 `StartLiveCaptionRequest` 與 `src/types/index.ts` 新增 session 級目標應用程式型別，包含根 PID 與啟動前複核所需識別資訊
- [ ] 4.3 於 `live_caption/mod.rs` 的 `validate_audio_source` 新增此來源，並於請求缺少目標時拒絕啟動
- [ ] 4.4 於 session 啟動分支新增此來源的擷取路徑，且音訊匯入既有共用佇列，不另建轉錄迴圈
- [ ] 4.5 確認指定應用程式來源同時適用 `add-streaming-incremental-caption` 的增量模式與關閉增量後的視窗模式

## 5. 前端介面

- [ ] 5.1 於 `src/components/liveCaptionSettings.ts` 的音訊來源選單新增「指定應用程式」
- [ ] 5.2 實作應用程式挑選介面（以名稱呈現，含重新整理）
- [ ] 5.3 加入提示文案：擷取範圍為整個應用程式而非單一分頁
- [ ] 5.4 加入提示文案：需先讓目標應用程式開始播放聲音才會出現在清單中
- [ ] 5.5 清單為空時顯示說明而非空白
- [ ] 5.6 開始 session 時以請求傳送當次選取目標；重載頁面或應用程式重啟後不得從設定檔還原舊 PID，失效時提示重新整理並重選
- [ ] 5.7 在用途說明中將此來源定位於直播、線上會議與其他即時內容；對可存取的非直播 YouTube 影片引導使用預先處理的影片字幕流程

## 6. 實測

- [ ] 6.1 以瀏覽器播放影片為目標實測，確認可擷取到聲音（驗證 `include_tree = true` 對多行程瀏覽器有效）
- [ ] 6.2 實測目標應用程式播放時，其他應用程式同時發聲不影響字幕內容
- [ ] 6.3 實測未選定目標即啟動時的拒絕行為與錯誤訊息
- [ ] 6.4 實測 session 進行中關閉目標應用程式的處理
- [ ] 6.5 實測切換回「電腦音訊（全系統）」與「麥克風」來源皆正常
- [ ] 6.6 實測應用程式仍執行但暫時停止輸出音訊時 session 維持運作，恢復播放後字幕可繼續
- [ ] 6.7 實測選取後關閉並重啟應用程式造成 PID 失效或重用時，啟動被拒絕且不會誤擷取其他行程
- [ ] 6.8 在增量模式啟用與關閉兩種情況下各實測一次指定應用程式來源，確認擷取來源不改變 LocalAgreement 或視窗式處理行為

## 7. 文件

- [ ] 7.1 於 README 的即時字幕說明加入此音訊來源，標註 Windows build 20348、PID 不持久化及「應用程式層級、非分頁層級」的能力邊界
- [ ] 7.2 說明可存取的非直播 YouTube 影片應優先使用預先處理的影片字幕流程，本來源用於直播、線上會議與其他即時內容
