## 1. 後端錄音基礎建設

- [x] 1.1 新增桌面錄音模組與 Windows 專用相依套件，建立裝置列舉與錄音 session manager 骨架
- [x] 1.2 增加 Tauri commands 與應用註冊，提供錄音模式裝置清單、開始錄音、停止錄音、取消錄音與暫存檔讀取能力
- [x] 1.3 擴充設定結構，保存預設錄音模式、麥克風裝置與系統音訊裝置偏好

## 2. 系統音訊與混音實作

- [x] 2.1 實作 Windows WASAPI loopback 系統音訊擷取流程，產出可預覽的暫存 WAV
- [x] 2.2 串接麥克風輸入與混音流程，讓混音模式輸出單一錄音檔並處理取樣格式對齊
- [x] 2.3 補上裝置失效、串流錯誤與手動停止時的部分檔案保留與錯誤回報

## 3. 前端錄音頁整合

- [x] 3.1 更新 `src/pages/record.ts`，加入錄音模式切換、來源選擇、平台支援提示與錄音前驗證
- [x] 3.2 更新 `src/api/recordings.ts` 與 `src/types/index.ts`，對應新的 Tauri commands、錄音狀態與設定欄位
- [x] 3.3 將停止錄音後的預覽與正式儲存改為使用後端暫存錄音檔，同時保留既有上傳音訊流程

## 4. 驗證與文件同步

- [x] 4.1 驗證僅麥克風、僅電腦音訊、混音模式與耳機會議情境的錄音結果
- [x] 4.2 確認新錄音檔可沿用既有逐字稿、校稿與摘要流程，且取消錄音會清理暫存檔
- [x] 4.3 更新與此功能直接相關的 README 或設定說明，標示目前為 Windows 優先支援與已知限制
