## 1. 備份後端基礎

- [x] 1.1 使用 Cargo 加入 ZIP 壓縮所需的 Rust 相依套件，並確認與現有 Tauri 及 Rust 版本相容。
- [x] 1.2 新增備份模組與版本化 manifest、明確定義的 schema revision 相容性、資產項目、匯入預檢及處理結果型別，且型別不包含設定或任何憑證欄位。
- [x] 1.3 實作 SQLite 一致性快照建立與暫存檔清理，確保 WAL 中已提交資料會包含在備份中。
- [x] 1.4 依快照的 recordings 與 archived meetings 收集存在的檔案，將資料庫快照、manifest 及資產先寫入同層暫存 ZIP，成功後原子置換目標檔，並在資產缺失時回傳警告而非中止備份。
- [x] 1.5 驗證 ZIP 內部項目使用安全的相對路徑，拒絕絕對路徑、路徑穿越、符號連結、重複項目與超出可用空間或安全上限的解壓內容。

## 2. 匯入與路徑還原

- [x] 2.1 實作備份 ZIP 預檢：驗證 manifest 格式與 schema revision 相容性、必要項目、SQLite 完整性與外鍵、資料與資產 ID 對應、解壓資源需求與目標可用空間，並回傳資料摘要與警告。
- [x] 2.2 實作受管理資產的串流暫存解壓、錄音還原至目前 AppData `recordings` 目錄、封存檔案還原至 `archives` 目錄，以 recording UUID 防止檔名碰撞，並重寫 `file_path`、`archived_path` 且不刪除外部自訂目錄內容。
- [x] 2.3 新增應用程式層級資料操作鎖定，讓匯入等待既有資料修改工作、拒絕新的資料修改 command，並保證成功或失敗後解除鎖定。
- [x] 2.4 將資料操作鎖定接入會議、分類、標籤、範本、常用參與者、錄音、轉錄、校稿、摘要、說話者對應與封存的所有資料修改 command，並保留純讀取 command 的可用性。
- [x] 2.5 實作覆蓋還原：先完成預檢、鎖定與暫存資產準備，再以交易置換現有資料，並在任何失敗時還原原本的資料與資產。
- [x] 2.6 實作合併匯入：略過既有 UUID 資料、依名稱重用分類與標籤、合併常用參與者使用次數，並回傳新增、略過、重用與警告統計。
- [x] 2.7 新增匯出、備份預檢與執行匯入 Tauri commands，於 `lib.rs` 註冊並確保 command 僅透過後端模組處理檔案與資料庫。

## 3. 前端操作介面

- [x] 3.1 在 `src/api/` 新增完整備份 API 封裝，並於 `src/types/index.ts` 定義備份、預檢與匯入結果型別。
- [x] 3.2 在設定頁新增「資料備份與還原」區塊，提供系統儲存對話框與建立 ZIP 備份操作。
- [x] 3.3 實作備份 ZIP 選取、預檢摘要、合併或覆蓋模式選擇與覆蓋二次確認流程。
- [x] 3.4 在備份與匯入處理期間停用重複操作，並以不含敏感資料的提示顯示成功、略過、重用與警告結果；匯入成功後重新取得或重新載入目前頁面資料。

## 4. 驗證

- [x] 4.1 為 manifest/schema 相容性、原子 ZIP 寫入、ZIP 路徑與資源限制驗證、SQLite 快照、缺失資產、檔名碰撞與路徑重寫新增 Rust 測試。
- [x] 4.2 為覆蓋還原與合併匯入建立整合測試，涵蓋不同 Windows 使用者目錄、UUID 衝突、分類與標籤重用、外部自訂目錄保留、資料操作鎖定及失敗回復。
- [x] 4.3 執行 `cargo test`、`cargo check` 與 `npm run build`，修正所有失敗後再次驗證。
