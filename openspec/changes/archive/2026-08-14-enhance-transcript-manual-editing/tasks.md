## 1. 逐字稿草稿模型與格式轉換

- [x] 1.1 在 `src/pages/meeting.ts` 定義僅供頁面內使用的結構化草稿列資料，包含錄音 ID、段落序號、可編輯時間、講者代號與文字，並沿用現有 `transcriptUtteranceRe` 格式規則
- [x] 1.2 實作從手動版與其 `manual_base_version` 分段內容建立草稿列的解析流程，保留錄音段落邊界，並在無法可靠對應時回報不可結構化而不修改原文
- [x] 1.3 實作草稿列序列化回既有手動逐字稿文字的流程，保留時間、講者、列順序、`no_break_before` 與錄音中場分隔語意
- [x] 1.4 驗證解析與序列化的往返結果：原始版基底、校稿版基底、單段、多段、無講者時間列及自由格式內容皆不得靜默遺失

## 2. 段落範圍講者識別

- [x] 2.1 建立以錄音 ID 與講者代號共同產生穩定 speaker scope 及色彩索引的 helper，並準備具可讀對比的循環色盤
- [x] 2.2 擴充逐字稿列渲染介面，使分段渲染時把 recording scope 套用至講者標籤，且段落標題或 badge 不只依賴顏色傳達錄音歸屬
- [x] 2.3 將相同 scope helper 套用至講者對應區，確認不同段落的同名講者使用不同樣式、同段落同講者維持一致
- [x] 2.4 讓講者對應候選來源包含已儲存手動版新增的講者代號，且仍以既有 `(recording_id, speaker_label)` API 儲存真人對應

## 3. 雙模式手動編輯流程

- [x] 3.1 重整全螢幕手動編輯狀態，使用單一草稿來源管理 `draftText`、可選 `draftRows`、目前編輯模式及最近已儲存內容
- [x] 3.2 在手動版編輯畫面加入「結構化編輯」與「純文字編輯」切換控制，保留既有 textarea，並確保模式切換不遺失尚未儲存的有效變更
- [x] 3.3 在純文字切回結構化時執行安全解析；若內容無法完整解析，維持純文字模式、保留全文並顯示明確提示
- [x] 3.4 統一兩種模式的儲存與取消行為：儲存沿用 `onSaveManualTranscript`，取消恢復最近已儲存內容，且原始版與校稿版不得被修改

## 4. 結構化列編輯功能

- [x] 4.1 建立依錄音段落分組的結構化 editor list，每列顯示可點擊及可編輯時間、講者選擇器與可編輯文字欄位
- [x] 4.2 由基底逐字稿、草稿列及既有講者對應建立各錄音段落的講者候選清單；有真人對應時顯示「人名（講者代號）」、未對應時顯示代號，並提供產生下一個合法且未使用 `講者X` 代號的動作
- [x] 4.3 實作列講者切換，立即更新草稿及段落範圍樣式，切換純文字或儲存後仍反映新講者
- [x] 4.4 實作所有列的時間編輯與驗證，支援既有時間格式，阻止格式錯誤、負值或超出所屬錄音長度的時間被儲存
- [x] 4.5 實作游標位置拆分：驗證前後皆有非空白內容；所屬錄音正在播放時以目前播放位置建立新列，否則沿用原列時間並標示可修改；保留前後文字並將焦點移至新列
- [x] 4.6 實作結構化列刪除：支援整理重複時間戳或短句後刪除列，保留相鄰列焦點，並阻止刪除錄音段落唯一列

## 5. 編輯中音訊播放

- [x] 5.1 將既有指定 recording ID 的時間跳轉能力提供給結構化 editor，每列時間戳只尋找其所屬 `audio[data-recording-id]`
- [x] 5.2 點擊時間戳時設定合法範圍內的播放位置並開始播放，沿用 audio `play` 事件的增益啟用與停止其他錄音行為
- [x] 5.3 所屬音訊元素、檔案或播放操作不可用時顯示非破壞性提示，不得 fallback 至第一段或其他錄音，並確認草稿不受影響
- [x] 5.4 確認編輯模式不建立第二套播放器，時間戳操作與既有會議錄音播放器共用同一 audio 元素及播放狀態

## 6. 全螢幕與響應式版面

- [x] 6.1 調整 `src/styles.css` 與全螢幕渲染流程，使完整逐字稿持續呈現於填滿 body 可用高度的內容區並自行捲動，頁尾固定，且不得以放大行距或列高掩蓋內容提早停止的問題
- [x] 6.2 設計結構化段落、編輯列、講者控制、拆分動作與模式切換樣式，沿用既有 VoxNote 視覺語言並維持鍵盤 focus 可辨識性
- [x] 6.3 加入窄視窗樣式，讓編輯列控制可換行、頁尾動作不被遮蔽，且全螢幕內容維持可閱讀與可捲動

## 7. 驗證與迴歸檢查

- [x] 7.1 執行 `npm run build`，修正 TypeScript 與 Vite 建置錯誤
- [x] 7.2 手動驗證單段與多段逐字稿：跨段落同名講者樣式不同、講者對應區一致、段落識別清楚，且有大量後續內容時完整渲染、可捲動、無異常空白並維持固定頁尾（待 Tauri 資料驗證）
- [x] 7.3 手動驗證結構化編輯的文字修改、「人名（講者代號）」選項、既有/新增講者切換、所有列時間修改、播放中/未播放拆分、指定段落時間播放、播放失敗，以及儲存後重新開啟（待 Tauri 資料與音訊驗證）
- [x] 7.4 手動驗證雙模式切換、自由格式 fallback、取消還原，並確認原始版、校稿版、複製、單檔匯出與整包匯出行為未回歸（待 Tauri 資料驗證）
- [x] 7.5 在桌面與窄視窗尺寸檢查捲動、鍵盤操作、焦點顯示、版本分頁及頁尾按鈕可用性（待含會議資料的 Tauri UI 驗證）
